export enum AjaxMethod {
  get = 'get',
  post = 'post',
  put = 'put',
  delete = 'delete',
  patch = 'patch',
}

export enum Status {
  pending = 'pending',
  success = 'success',
  fail = 'fail',
}
